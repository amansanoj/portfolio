---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title="GardenX — Case Study"
			description="How I built an AI-powered marketplace enabling schools to commercialize student-grown organic produce."
		/>
	</head>
	<body>
		<Header />
		<main id="main-content">

			<!-- ── Header ──────────────────────────────────────── -->
			<section class="case-hero">
				<a href="/about#projects" class="back-link">
					<i class="ph-fill ph-arrow-left"></i> Back to Projects
				</a>
				<h1>GardenX</h1>
				<p class="tagline">AI-powered marketplace for school-grown organic produce</p>
				<div class="meta-row">
					<span><i class="ph-fill ph-calendar"></i> Dec 2024 – Mar 2025</span>
					<span><i class="ph-fill ph-map-pin"></i> Dubai, UAE</span>
				</div>
				<div class="tags">
					<span class="tag">AI</span>
					<span class="tag">React.js</span>
					<span class="tag">Node.js</span>
				</div>
			</section>

			<!-- ── Overview ─────────────────────────────────────── -->
			<section class="section">
				<h2>Overview</h2>
				<p>
					GardenX is a platform that enables schools to commercialize produce grown by
					students in school gardens. It uses AI to personalize orders and streamline
					inventory management, creating a closed-loop marketplace between schools and buyers.
				</p>
				<p>
					Within 4 months of launch, the platform facilitated over 30,000 AED in produce
					sales.
				</p>
			</section>

			<!-- ── Problem ──────────────────────────────────────── -->
			<section class="section">
				<h2>The Problem</h2>
				<p>
					<!-- Describe the specific gap or pain point you were solving. -->
					Schools with active gardening programs had no efficient way to sell excess produce.
					Distribution was manual, inconsistent, and disconnected from demand.
				</p>
			</section>

			<!-- ── Solution ─────────────────────────────────────── -->
			<section class="section">
				<h2>The Solution</h2>
				<p>
					<!-- Explain your approach and key architectural decisions. -->
					A centralized web platform with three core components:
				</p>
				<ul>
					<li>
						<strong>AI-driven order personalization</strong> — recommends produce bundles
						based on buyer history and seasonal availability.
					</li>
					<li>
						<strong>Inventory management dashboard</strong> — gives school admins real-time
						visibility into stock levels and pending orders.
					</li>
					<li>
						<strong>App-based order fulfillment</strong> — buyers place and track orders
						from a mobile-friendly interface.
					</li>
				</ul>
			</section>

			<!-- ── Impact ───────────────────────────────────────── -->
			<section class="section">
				<h2>Impact</h2>
				<div class="stats-row">
					<div class="stat">
						<span class="stat-value">30,000+ AED</span>
						<span class="stat-label">produce sales in 4 months</span>
					</div>
					<div class="stat">
						<span class="stat-value"><!-- N schools --></span>
						<span class="stat-label">schools onboarded</span>
					</div>
					<div class="stat">
						<span class="stat-value"><!-- N orders --></span>
						<span class="stat-label">orders fulfilled</span>
					</div>
				</div>
			</section>

			<!-- ── Learnings ─────────────────────────────────────── -->
			<section class="section">
				<h2>What I Learned</h2>
				<p>
					<!-- Reflect on technical, product, and personal takeaways. -->
					Placeholder — add your key learnings here.
				</p>
			</section>

			<!-- ── Next Steps ────────────────────────────────────── -->
			<section class="section last">
				<h2>What's Next</h2>
				<p>
					<!-- Future plans, handoff, or status. -->
					Placeholder — add future roadmap or current status here.
				</p>
			</section>

		</main>
		<Footer />
	</body>
</html>

<style>
	main {
		max-width: 760px;
		width: 100%;
		box-sizing: border-box;
		margin: 0 auto;
		padding: 2em 1.5em 4em;
	}

	/* ── Case Hero ─────────────────────────────────────── */
	.case-hero {
		padding: 3em 0 2.5em;
		border-bottom: 1px solid rgb(var(--gray-light));
	}
	.back-link {
		display: inline-flex;
		align-items: center;
		gap: 0.35em;
		font-size: 0.88em;
		font-weight: 600;
		color: rgb(var(--gray));
		text-decoration: none;
		margin-bottom: 1.25em;
		transition: color 0.15s;
	}
	.back-link:hover { color: var(--accent); }
	.case-hero h1 {
		margin: 0 0 0.2em;
	}
	.tagline {
		font-size: 1.05em;
		color: rgb(var(--gray));
		margin: 0 0 1em;
	}
	.meta-row {
		display: flex;
		flex-wrap: wrap;
		gap: 1.2em;
		font-size: 0.88em;
		color: rgb(var(--gray));
		margin-bottom: 1em;
	}
	.meta-row span {
		display: flex;
		align-items: center;
		gap: 0.3em;
	}
	.tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5em;
	}
	.tag {
		background: rgb(var(--gray-light));
		color: rgb(var(--gray-dark));
		padding: 0.25em 0.75em;
		border-radius: 999px;
		font-size: 0.82em;
		font-weight: 500;
	}

	/* ── Sections ───────────────────────────────────────── */
	.section {
		padding: 2em 0;
		border-bottom: 1px solid rgb(var(--gray-light));
	}
	.section.last { border-bottom: none; }
	.section h2 {
		font-size: 1.15em;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.06em;
		color: rgb(var(--gray));
		margin: 0 0 1em;
	}
	.section p, .section li {
		color: rgb(var(--gray-dark));
		line-height: 1.7;
	}
	.section ul {
		padding-left: 1.25em;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.5em;
	}

	/* ── Stats ──────────────────────────────────────────── */
	.stats-row {
		display: flex;
		flex-wrap: wrap;
		gap: 1.5em;
	}
	.stat {
		display: flex;
		flex-direction: column;
		gap: 0.2em;
		background: rgb(var(--gray-light));
		border-radius: 10px;
		padding: 1em 1.5em;
		min-width: 140px;
	}
	.stat-value {
		font-size: 1.4em;
		font-weight: 700;
		color: rgb(var(--black));
	}
	.stat-label {
		font-size: 0.82em;
		color: rgb(var(--gray));
	}

	@media (max-width: 600px) {
		main { padding: 1.5em 1em 3em; }
		.case-hero { padding: 2em 0 1.5em; }
		.stats-row { flex-direction: column; }
		.stat { min-width: unset; }
	}
</style>
